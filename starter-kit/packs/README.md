# Packs

Packs are overlays applied on top of `starter-kit/core/`.

- `packs/verticals/*` add industry workflows, site-record patterns, validation scenarios.
- `packs/personas/*` add behavioral/orchestration guidance and defaults.

A pack should never contain secrets or runtime identity.
