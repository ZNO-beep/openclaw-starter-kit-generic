# Changelog — youtube_search

## 2026-02-01
- Added executable implementation: `tool.js`.
- Switched implementation to use Node’s global `fetch` (no external deps).
- Added pagination controls: `--pages` and `--maxTotal` (maxResults is now per-page).
- Updated `TOOL.md` interface docs and updated `tools-index.md`.
