#!/usr/bin/env bash
set -euo pipefail

# Finance pack install
# This pack currently installs only docs/scripts into the repo; it does not modify runtime state by default.
# (Site automations should be executed via Playwright templates + case wrapper.)

echo "Finance pack: no runtime install steps yet (docs/playbooks only)."
echo "Use the site records and Playwright wrapper to run finance workflows."
